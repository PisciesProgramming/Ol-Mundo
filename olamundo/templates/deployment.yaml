apiVersion: apps/v1
kind: Deployment
metadata:
  name: olamundoapi
  namespace: default
  labels:
    app: olamundoapi
spec:
  replicas: 5
  selector:
    matchLabels:
      app: olamundoapi
      tier: frontend
  template:
    metadata:
      labels:
        app: olamundoapi
        tier: frontend
    spec: # Pod spec
      containers:
      - name: mycontainer
        image: asdkant/fastapi-hello-world
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: myconfigmapv1.0
        resources:
          requests:
            memory: "16Mi" 
            cpu: "50m"    # 500milliCPUs (1/2 CPU)
          limits:
            memory: "128Mi"
            cpu: "100m"
